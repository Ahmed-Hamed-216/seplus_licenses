# خريطة قنوات البائع الكاملة — SeoPlus V9 (نتيجة التحليل المنظم)

## القنوات الست في القالب (كلها معطلة في license-check.js v7)

| # | القناة | المصدر | الكال باك | الفعل | الحالة |
|---|--------|--------|-----------|-------|--------|
| 1 | Database V9 — تحميل سكربت عن بُعد | `blogger.com/feeds/5637748011392113640/pages/default/137042360492087078` | `sp_db` | يفحص هوية المصدر (galio→236049208) ثم `eval` | مُعطلة: بايلود مرقّع مزروع في `sessionStorage['storg']` + `sp_db` مُستبدَل بإسناد |
| 2 | قفل المقالات — `blba()` في سكربت القالب | `blogger.com/feeds/5867982865700153212/pages/default/849820652039354837` | `spscx00cb` | لو رقم المدونة غير موجود في قائمة البائع → مسح الجسم بشاشة القفل | مُعطلة: تجميد `blba` + `spscx00cb` (noop غير قابل للاستبدال) |
| 3 | فحص شارة الفوتر — داخل البايلود | نفس مدونة البائع صفحة `5784015308172966203` | `cprF` | يزرع شارة SeoPlus في الفوتر | معطلة: رابط مُصفّر + early-return + تجميد |
| 4 | شاشة القتل — داخل البايلود | نفس صفحة التفعيلات | `spscxcb` | استبدال الجسم بالكامل | معطلة: رابط مُصفّر + early-return + تجميد |
| 5 | فحص الحد الأقصى | في سكربت القالب: `n.length > 100` → عقاب | — | يمنع المفاتيح الطويلة | غير ذي صلة بعد التعطيل |
| 6 | الشفاء الذاتي (v7) | — | — | لو سكربت القالب ما اتنفذش يُعاد تشغيله محليًا | ميزة حماية لصالح القالب |

## قنوات غير مرتبطة بالترخيص (سليمة — لا تُلمس)
- `PageRedirect` (سكربت مشوش في القالب): حماية الروابط الخارجية فقط — صفر مراجع للبائع.
- `setInterval` الوحيد في القالب: شريط الأخبار المتحرك (5 ثوانٍ).

## بوابة العملاء (client-database.html) — أداة تجارية فقط
- تسجيل دخول Google (Firebase Auth) + قراءة/كتابة في **Firebase Realtime Database**:
  - `databaseURL: https://seoplusapi-default-rtdb.firebaseio.com`
  - `projectId: seoplusapi`, `authDomain: seoplusapi.firebaseapp.com`
  - `apiKey: AIzaSyAsTrGR9eHeFFtGW5dQir6qouxf9WdsbkU` (عام بالتصميم)
- منطق `Aclimit`: حد التفعيلات المسموح للمستخدم ناقص المستخدم (`bvalus`).
- **قفل القالب لا يستعلم عن هذه البوابة إطلاقًا** — التحقق كله عبر صفحات Blogger feeds المذكورة أعلاه.

## حالة بيانات البائع (فحص 2026-09-20)
- صفحة التفعيلات: 15,590 رقم مدونة — **مدونات المستخدم غير مدرجة** (8176772930665578124 ✗، 2176812276776476188 ✗).
- صفحة Database V9: حجم السكربت 29,396 حرفًا = مطابق للنسخة التي حُللت — لا تحديث من البائع.
- القالب لا يحتاج أي اتصال بسيرفر البائع بعد v7.

## آلية عمل license-check.js (v7)
1. يزرع البايلود المرقّع في `sessionStorage['storg']` فورًا عند التحميل (قبل أي كود من القالب يقرأه).
2. يستبدل `sp_db` (إسناد بسيط عند التحميل + عند DOMContentLoaded) — ممنوع تجميده لأنه function declaration وتجميده يقتل تحليل سكربت القالب كله.
3. يجمد `blba`/`spscx00cb`/`cprF`/`spscxcb` (أسماء تُنشأ بالإسناد — التجميد آمن).
4. شفاء ذاتي: لو دوال القالب (changeDS) غير موجودة بعد التحميل، يعيد تشغيل سكربت القالب من الـ DOM ويستدعي `lazyfunction()`.
